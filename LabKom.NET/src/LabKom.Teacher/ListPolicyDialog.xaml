<Window x:Class="LabKom.Teacher.ListPolicyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="520" Height="520"
        MinWidth="420" MinHeight="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#0F172A"
        Foreground="#E2E8F0"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeadingText"
                   FontSize="22"
                   FontWeight="SemiBold" />
        <TextBlock x:Name="InstructionText"
                   Grid.Row="1"
                   Margin="0,6,0,14"
                   Foreground="#94A3B8"
                   TextWrapping="Wrap" />
        <TextBox x:Name="ValuesText"
                 Grid.Row="2"
                 AcceptsReturn="True"
                 AcceptsTab="False"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"
                 FontFamily="Consolas"
                 FontSize="13"
                 Padding="10"
                 Background="#111827"
                 Foreground="#E2E8F0"
                 BorderBrush="#334155" />
        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="ExampleText"
                       Grid.Column="0"
                       Foreground="#64748B"
                       FontSize="10"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1"
                    Content="Batal"
                    MinWidth="88"
                    Padding="12,7"
                    Margin="8,0"
                    IsCancel="True" />
            <Button Grid.Column="2"
                    Content="Terapkan"
                    MinWidth="96"
                    Padding="12,7"
                    IsDefault="True"
                    Click="ApplyButton_OnClick" />
        </Grid>
    </Grid>
</Window>